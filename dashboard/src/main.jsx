import React from "react";
import ReactDOM from "react-dom/client";
import PredictionDashboard from "./PredictionDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PredictionDashboard />
  </React.StrictMode>
);
